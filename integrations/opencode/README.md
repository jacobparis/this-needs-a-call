# OpenCode Integration

Reserved for a future OpenCode adapter.

Expected shape:

- Configure OpenCode to reach the user's deployed `/mcp` endpoint with bearer auth.
- Provide a command or prompt equivalent to `this-needs-a-call`.
- Reuse authorized `POST /api/call-session` for session creation.

This integration is not implemented yet.
